{
  "name": "assetize",
  "version": "0.0.1",
  "description": "",
  "private": false,
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "bin": {
    " ": "dist/main.js"
  },
  "scripts": {
    "test": "vitest run",
    "build": "tsup src/index.ts --format cjs,esm  --dts",
    "lint": "tsc",
    "test:setup": "yarn unlink --global assetize && yarn link --global assetize",
    "setup": "chmod +x ./dist/main.js",
    "release": "pnpm run lint && pnpm run test && pnpm run build && changeset publish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "tsup": "^8.0.2",
    "typescript": "^5.3.3",
    "vitest": "^1.3.1"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@clack/prompts": "^0.7.0",
    "@types/node": "^20.11.24",
    "picocolors": "^1.0.0"
  }
}